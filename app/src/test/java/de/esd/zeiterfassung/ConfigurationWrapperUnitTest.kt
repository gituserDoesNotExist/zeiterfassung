package de.esd.zeiterfassung

import org.junit.Test

import org.junit.Assert.*

/**
 * ConfigurationWrapper local unit daysWithoutChocolate, which will execute on the development machine (host).
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 */
class ConfigurationWrapperUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
